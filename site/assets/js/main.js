/* ============================================================
   Corvo Bianco — main.js
   ============================================================
   Volutamente minimo. Sumi-e applicato al codice: solo i tratti
   necessari. Nessun framework, nessuna dipendenza.

   Per ora: nessuna interazione attiva. Le pagine sono leggibili
   senza JavaScript. Questo file esiste come punto di estensione
   futuro (es. smooth scroll, lazy loading immagini, eventuale
   theme toggle).
   ============================================================ */

(function () {
  'use strict';

  // Punto di estensione: aggiungere qui future micro-interazioni
  // mantenendo il principio di sottrazione. Nessuna animazione
  // decorativa, nessun "wow factor".

  // Esempio di pattern (commentato, attivare se servisse):
  //
  // document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
  //   anchor.addEventListener('click', (e) => {
  //     const target = document.querySelector(anchor.getAttribute('href'));
  //     if (target) {
  //       e.preventDefault();
  //       target.scrollIntoView({ behavior: 'smooth', block: 'start' });
  //     }
  //   });
  // });

})();
